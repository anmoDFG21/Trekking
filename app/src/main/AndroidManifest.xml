<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trekking"
        tools:targetApi="31">
        <activity
            android:name=".AttivitaActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Trekking">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity
            android:name=".ProvinciaActivity">
        </activity>



        <activity
            android:name=".BresciaActivity">
        </activity>

        <activity
            android:name=".ComoActivity">
        </activity>

        <activity
            android:name=".LeccoActivity">
        </activity>

        <activity
            android:name=".SondrioActivity">
        </activity>
        <activity
            android:name=".BergamoActivity">
        </activity>

    </application>


</manifest>